{
  "name": "http-requester",
  "version": "0.1.0",
  "main": "src/index.js",
  "scripts": {
    "serve": "five-server --ignore=node_modules,.git --no-browser"
  },
  "dependencies": {
    "five-server": "^0.4.5"
  }
}
